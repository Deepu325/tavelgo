# Phase 1 Plan: 01-02 — Database Schema Design & Advanced Middleware

## Goal
Finalize the core database schema (Rides and Vehicles) and implement advanced middleware to support the booking lifecycle.

## Tasks

### Wave 1 — Schema Expansion
- [ ] Create `backend/models/Vehicle.js` — tiers: 5-Seater, Innova Crysta, Tempo Traveller.
- [ ] Create `backend/models/Ride.js` — tracking pickup, destination, fare, status, and participants.
- [ ] Add `status` enum to `Ride` model: `pending`, `accepted`, `ongoing`, `completed`, `cancelled`.
- [ ] Add `driverId` and `customerId` references to `Ride` model.

### Wave 2 — Middleware & Logic Refinement
- [ ] Refine `backend/middleware/roleCheck.js` to handle complex permission sets if needed.
- [ ] Create a seed script `backend/scripts/seedVehicles.js` to populate vehicle tiers.
- [ ] Update `backend/server.js` to include the new routes (placeholder for now).

### Wave 3 — Verification
- [ ] Verify `Ride` model validation (e.g., cannot create ride without customer).
- [ ] Verify `Vehicle` model unique tiers.
- [ ] Run seed script and verify database entries.

## Files Created/Modified
- `backend/models/Vehicle.js` [NEW]
- `backend/models/Ride.js` [NEW]
- `backend/scripts/seedVehicles.js` [NEW]
- `backend/routes/rides.js` [NEW]
- `backend/routes/vehicles.js` [NEW]
